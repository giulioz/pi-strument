#ifndef GRAPHICS_H
#define GRAPHICS_H

#include "colors.h"
#include "sansserif.h"

void printString(const char *str, int offsetX, int offsetY);
void printChars(const char *str, int length, int offsetX, int offsetY);

#endif
